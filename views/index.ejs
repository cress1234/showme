<!DOCTYPE html>
<html>

  <head>

    <meta charset=utf-8>
    <meta http-equiv="X-UA-Compatible" content="IE-edge">
    <meta name="viewport" content="width-device-width, initial-scale=1">
    <title>ShowMe! Test</title>
    <!-- stylesheets -->
    <link rel="stylesheet" type="text/css" href="style.css">
  </head>

  <body>
  <!-- Objects to draw -->
    <div id="outer">
        <h1> ShowMe! </h1>
        <div id="inner">
            <p> Select a random TV show by clicking the button below. </p>
            <div id="buttons">
              <form action="/" method="post">
                <input name="show" id="output" onfocus="clearInput()">
                <button type="submit" onclick="addShow()">Add show</button>
              </form>
              <!-- currently does nothing -->
              <% if(details !== null){ %>
                <p><%= details %></p>
              <% } %>
              <% if(error !== null){ %>
                <p><%= error %></p>
              <% } %>
              <button onclick="getShow()">Get show</button>
            </div>
        </div>
    </div>
  </body>
</html>
